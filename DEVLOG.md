# T03 개발 기록

## 초기 결정 (2026-09-22)

- 빈 저장소에서 시작하므로 설치 없이 실행되는 HTML/CSS/Canvas 구조를 선택했다.
- 레퍼런스 이미지는 복제하지 않고, 디카·포토스트립·필름·체크 패턴을 Canvas 도형으로 직접 그렸다.
- 스티커는 외부 이모지 대신 직접 작성한 단색 SVG path를 Canvas에 그려 연필선 인상을 유지했다.
- 자동 저장에는 설정만 기록하되, 사용자가 내려받는 JSON 백업에는 사진 원본과 각 칸의 크롭 위치를 함께 기록한다.

## 안전/검증

- 허용 MIME: image/png, image/jpeg
- 출력 크기: 1080×1080, 1080×1350, 1080×1920
- JSON은 version/state/templates 필수 항목 및 템플릿 속성을 모두 확인한 뒤 한 번에 반영한다.
- JSON 검증 실패 시 메모리와 localStorage를 변경하지 않는다.
- AI는 기획·코드 초안·검증에 사용했고, 결과물의 문구/레이아웃/기능은 과제 기준에 맞춰 직접 판단했다.

## 자유 편집 마무리 (2026-09-23)

- 현재 버전은 제공된 레퍼런스 원본 16장으로 프레임 6종과 스티커 140종을 구성한다. 원본 배경·워터마크가 포함된 소재는 목록에 이를 표시한다.
- 스티커를 독립 레이어로 추가하고 이동·크기·회전·불투명도·반전·복제·삭제·쌓임 순서 변경을 지원한다. 캔버스 크기/회전 핸들, 방향키, Ctrl/Cmd+Z 및 Shift+Z도 동작한다.
- 문구 이동·크기·색·줄바꿈·회전, 프레임 이동·크기·자체 중심 회전, 사진 확대·회전·칸 교환·교체·삭제를 지원한다.
- 사진 업로드·교체, 비율·템플릿 변경 등의 실행 취소 누락을 수정했다. 템플릿 CRUD도 되돌릴 수 있도록 템플릿 목록을 편집 이력에 함께 보관한다.
- 회전한 문구 선택, 회전한 프레임의 이동, 선택 레이어와 조절 값의 동기화를 수정했다. 샘플 선택 시 올바른 프레임 ID를 적용한다.
- 사용자 이미지 스티커와 사진은 브라우저에 자동 저장된다. 사용자 템플릿에는 꾸미기 설정이, JSON 작업 백업에는 사진 원본까지 저장된다. 초기 버전의 ‘이미지 원본을 localStorage에 넣지 않음’ 정책을 변경하고 화면 안내를 맞췄다.
- JSON 편집 값·레이어·템플릿·이미지를 검증하고 디코딩을 마친 뒤 반영한다. 손상된 이미지나 템플릿 저장 공간 부족 시 기존 작업을 유지한다.
- 검증: Node 테스트 11개 통과. Edge 브라우저에서 `scripts/qa-browser.cjs`의 19개 기능 흐름 통과, 런타임 오류 0건. 320/390/768/1440px 가로 넘침 없음.
- PNG 1080×1080 / 1080×1350 / 1080×1920 출력 확인. 선택 테두리를 제거한 미리보기와 PNG 이미지 데이터가 정확히 일치한다.
- 공개 배포 대상: https://studio4.rnjswnghl.chatgpt.site (기존 Sites 프로젝트와 공개 범위를 유지).
